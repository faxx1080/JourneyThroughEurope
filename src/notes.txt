Game plan:
Save, load
CPU.

Save on Button press.

On call, need to save:

numcards
currentMessage
currentPlayer int
dice (Object)
	rolledSix
fliedAlready
gameState
logger.getText()
movesLeft
nextRedCard.getId() **lookup later

int: numPl
Player info:
	number
	name
	isCPU
	homeCity
	currentCity
	//restriction = new nothing.
	int nCards
	cards(id)
	int prevCities
	prevCities(id)
	
pass in numcards, diceroll, fliedAlready, gameState, plNames, cpuPlayers